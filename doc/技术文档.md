# P2PChat 技术文档

本文档描述 P2PChat 的架构设计、核心流程、数据结构与关键实现细节，供二次开发与维护参考。

## 1. 总体架构

P2PChat 是一个**单文件纯前端应用**（`P2PChat.html`），所有 HTML、CSS、JavaScript 内联在一个文件中，无外部依赖、无构建步骤、无后端服务。

### 1.1 技术选型

| 能力 | 方案 |
|------|------|
| 传输通道 | WebRTC `DataChannel`（有序可靠模式） |
| 加密 | DTLS（WebRTC 强制，端到端） |
| 信令 | 手动复制粘贴连接码（base64 编码的 JSON） |
| NAT 穿透 | 无（仅直连模式，不使用 STUN/TURN）；仅依赖 host 候选，限同一局域网或双方均有公网 IP |
| 持久化 | `localStorage` 自动保存 + JSON 文件导出/导入 |
| UI | 原生 DOM + 响应式 CSS（桌面双栏 / 移动单栏） |

### 1.2 为什么是「手动信令」

浏览器安全沙箱有两点根本限制：
1. 无法用 IP 建立原始 TCP/UDP 连接；
2. 无法监听入站连接。

因此「输入 IP 即直连」在纯前端不可行。WebRTC 是唯一可行的浏览器 P2P 技术，但其建立连接需要一次信令交换（交换 SDP offer/answer）。在无后端的前提下，采用**手动复制粘贴连接码**完成信令。

### 1.3 固有限制

WebRTC 的 SDP（含 DTLS 指纹、ICE ufrag/pwd）是**每次会话临时生成**的，无法跨会话复用。因此：
- 已保存的联系人**无法凭存储的 IP 自动重连**；
- 每次重新连接仍需双方**重新交换一次连接码**。

联系人列表中的 IP/备注仅用于识别与整理，不用于「拨号」。

### 1.4 候选类型与 mDNS

ICE 连通依赖候选（candidate）：

| 候选 | 来源 | 受 mDNS 影响 |
|------|------|--------------|
| `host` | 本机网卡 IP | 是，被混淆为 `*.local` |
| `srflx` | STUN 反射地址 | 否 |
| `relay` | TURN 中继 | 否 |

仅直连模式 `iceServers: []`，ICE 只能收集 `host` 候选。Chrome 默认开启 mDNS 混淆，将 host 候选改写为 `*.local`，对端在不同子网 / 组播受限 / 不同浏览器实例时常解析失败，导致连接**静默失败**（ICE 不报错，只是连不通）。因此仅直连模式下，**关闭 mDNS 混淆是连通前提**（详见 §8）。有 STUN 时此问题不存在——`srflx` 候选为 STUN 返回的真实地址，不依赖 host，故 mDNS 开关与否均能连通。

## 2. 连接流程（三步手动信令）

为简化交互，**关闭 trickle ICE**，等 ICE 收集完成后读取完整 `localDescription`（含全部候选）再编码，使每方只需复制一段文本。

```
邀请方 A                         被邀方 B
─────────                       ─────────
createDataChannel('chat')
createOffer + setLocalDescription
wait iceGatheringState==='complete'
生成【邀请码】 ────────────────▶ 粘贴邀请码
                                  setRemoteDescription(offer)
                                  ondatachannel 监听
                                  createAnswer + setLocalDescription
                                  wait ICE complete
                  ◀────────────  生成【应答码】
粘贴应答码
setRemoteDescription(answer)
                ▼ ICE 连通 / DTLS 握手 ▼
            DataChannel open，互发 hello
                ▼ 双向加密聊天 ▼
```

### 连接码格式

```
base64( JSON{ type, sdp, identity } )
```

- `type`：`'offer'` 或 `'answer'`
- `sdp`：`RTCSessionDescription`（含所有 ICE 候选）
- `identity`：`{ id, name }`，本机身份

编码采用 `btoa(unescape(encodeURIComponent(...)))` 以兼容中文等 UTF-8 字符；解码对称。

### 身份解析时机

- **被邀方**：邀请码中已含邀请方 `identity`，`ondatachannel` 时即可知对端身份，通道打开后立即登记联系人。
- **邀请方**：应答码中虽含被邀方身份，但邀请方在 `finalizeOffer` 阶段未解析它；邀请方需等对端通过 DataChannel 发来 `hello` 消息才能确认身份。故邀请方通道打开后先「等待对端身份」，收到 `hello` 再登记。

## 3. 数据结构

### 3.1 持久化结构（`localStorage` key: `ipvchat_data`）

```js
{
  version: 2,
  identity: { id, name },                 // 本机身份
  contacts: [
    { id, name, ip, lastSeen, note, nameSet }
  ],
  messages: { [contactId]: [ { ts, dir, text } ] },
  settings: {}                              // v4 起仅直连，无 STUN 配置
}
```

字段说明：
- `identity.id`：随机生成的本机唯一标识，随连接码发送给对端，作为联系人去重键。
- `contacts[].id`：对端身份 id（非 IP），用于匹配连接。
- `contacts[].nameSet`：是否用户已自定义备注名；为 `true` 时对端 `hello` 中的名字不会覆盖。
- `messages[].dir`：`'in'` / `'out'` / `'sys'`（系统提示）。
- `settings`：v4 起仅直连模式，不再保存 STUN 相关配置。

### 3.2 运行时状态（非持久化）

| 变量 | 说明 |
|------|------|
| `store` | 加载自 localStorage 的全局数据对象 |
| `currentId` | 当前选中联系人 id |
| `pendingPC` | 握手中的 `RTCPeerConnection` |
| `connections` | `Map<contactId, channel>`，已确认身份的活跃连接 |
| `channelMap` | `Map<channel, {pc, contactId|null}>`，通道→连接信息反查 |

## 4. 模块设计

### 4.1 WebRTC 层

- `newPC()`：创建 `RTCPeerConnection`，`iceServers` 取自 `store.settings`（直连模式时为空数组）。
- `waitIceComplete(pc)`：Promise，等 `iceGatheringState==='complete'`，带 4 秒兜底超时。
- `bindChannel(channel, pc, knownPeerId, knownPeerName)`：绑定通道事件（open/message/close/error）与 ICE 状态监听。
- `finalizeChannel(channel, peerId, peerName)`：身份确认后登记连接、创建/匹配联系人、探测对端 IP、切换 UI。
- `detectPeerIp(pc, contactId)`：通过 `pc.getStats()` 读取选中的 candidate-pair，解析对端地址写入联系人。

### 4.2 消息协议（DataChannel 传输 JSON）

| type | 字段 | 说明 |
|------|------|------|
| `hello` | `identity` | 连接建立后互发，交换身份 |
| `msg` | `ts, text` | 聊天消息 |
| `bye` | — | 主动断开通知 |

身份未确认（`contactId` 为 null）前，业务消息被丢弃。

### 4.3 存储层

- `loadStore()` / `saveStore()`：读写 localStorage，带 try/catch 容错。
- 版本迁移：见 §6。
- `exportJSON()`：`Blob` 下载 `p2pchat-backup-YYYYMMDD.json`。
- `importJSON()`：`<input type=file>` 读取，合并/覆盖后写入并刷新 UI。

### 4.4 本机地址探测

`refreshMyIp()`：创建临时 `RTCPeerConnection`（`iceServers: []`，仅直连），收集 ICE host 候选：
- `host` 候选 → 本机地址（IPv4/IPv6）；
- `*.local` → mDNS 混淆名，单独提示（无 STUN，故不产生 srflx 公网候选）。

`fe80::` 开头标注「链路本地」。仅显示本机地址。

### 4.5 UI 渲染

- `renderAll()` / `renderContacts()` / `renderChat()` / `renderTopbar()` / `renderMessages()`：分层渲染。
- `updateMobileView()`：根据 `currentId` 在 `#app` 上切换 `show-chat` 类，驱动移动端滑入式视图。
- 对话框：连接向导、联系人详情、设置，均为 overlay 模态。

## 5. 响应式布局

- 桌面端（>680px）：侧栏 300px + 主区 flex，双栏。
- 移动端（≤680px）：
  - `#app` 改为纵向；`#main` 绝对定位、`translateX(100%)` 隐藏；
  - 选中联系人时 `#app.show-chat #main` 滑入，侧栏隐藏；
  - 顶栏显示「‹」返回按钮；
  - 模态对话框改为底部抽屉式。

## 6. 版本迁移

`store.version` 用于数据格式升级：

| 版本 | 变更 |
|------|------|
| 1 | 初始版本，默认启用 STUN |
| 2 | 默认改为直连模式（禁用 STUN） |
| 3 | STUN 列表替换为国内可达服务器 |
| 4 | 彻底移除 STUN/TURN，仅保留直连；忽略历史 STUN 配置 |

迁移逻辑同时存在于 `loadStore()`（启动加载）与 `importJSON()`（导入备份）。v4 起统一置 `version=4` 并忽略任何历史 STUN 配置，始终为仅直连模式。

## 7. 安全模型

### 7.1 传输安全
DataChannel 强制使用 DTLS。SDP 中的 DTLS 指纹在握手时由浏览器校验，所有数据端到端加密。仅直连模式下流量纯 P2P，不经过任何中转。

### 7.2 信令安全
连接码含 DTLS 指纹与会话凭证。本程序能防**被动窃听**，但**不能单独防主动中间人**：若攻击者能在不可信信道上篡改连接码，可替换为自身码进行转发。缓解措施：
- 通过可信渠道交换连接码；
- 或与对方口头核对一段短指纹（本程序未实现指纹显示，可作为扩展点）。

### 7.3 本地存储
`localStorage` 与导出 JSON 均为**明文落盘**，传输加密不覆盖本机存储。建议妥善保管导出文件。

## 8. 已知限制与扩展方向

| 限制 | 说明 | 可能扩展 |
|------|------|----------|
| 无法自动重连 | SDP 每次临时生成 | 引入轻量信令服务器实现「输入 ID 即连」 |
| 跨 NAT 不同网络无法直连 | 仅直连无 STUN/TURN，host 候选不可跨网路由 | 需要时另引入 STUN/TURN 或信令服务器 |
| mDNS 混淆 | Chrome 默认将 host 候选改写为 `*.local`，仅直连模式下对端解析失败致连不上 | 连通前提：在 `chrome://flags/#enable-webrtc-hide-local-ips-with-mdns` 关闭 mDNS 混淆（见 §1.4） |
| 无文件/音视频传输 | 仅文本 | 基于 DataChannel 扩展文件分片传输 |
| 无群聊 | 仅一对一 | 需引入 mesh 或 SFU 架构 |
| 无指纹核对 | 信令防 MITM 依赖信道可信 | UI 展示 DTLS 指纹供人工核对 |

## 9. 浏览器兼容性

主测 Chrome / Edge（Chromium 内核），兼顾 Firefox。需支持 WebRTC DataChannel、`localStorage`、`RTCPeerConnection.getStats()`。`file://` 协议下均可运行；剪贴板复制带 `execCommand` 回退以兼容非安全上下文。
